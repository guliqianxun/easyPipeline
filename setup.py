from setuptools import setup, find_packages

setup(
    name='easyPipeline',
    version='2.0.0.0',
    author='Zhiheng Liu',
    author_email='visitorindark@gmail.com',
    description='A simple pipeline framework, with batch execution support',
    packages=find_packages(),
    install_requires=[
        
    ],
)
